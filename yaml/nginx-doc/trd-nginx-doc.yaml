apiVersion: apps/v1
kind: Deployment
metadata:
  name: trd-prd-doc
  namespace: trd-frontend
  labels:
    app: trd-doc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: trd-doc
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
       app: trd-doc
    spec:
      containers:
      - image: 
        name: doc
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: 256Mi
            cpu: 200m
          limits:
            memory: 256Mi
            cpu: 200m
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: pv-trd-doc
          mountPath: /trd/doc
      volumes:
      - name: pvc-trd-doc
        persistentVolumeClaim:
          claimName: pvc-doc
      nodeSelector:
       app: trd-doc

---

apiVersion: v1
kind: Service
metadata:
  name: trd-doc-svc
spec:
  selector:
    app: trd-doc
  type: NodePort
  clusterIP: 10.100.100.104
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 34000
